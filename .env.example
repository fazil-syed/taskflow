# Port the app is published on
APP_PORT=8080

# MySQL credentials (change these if this is reachable from anywhere but localhost)
MYSQL_DATABASE=taskflow
MYSQL_USER=taskflow
MYSQL_PASSWORD=taskflow
MYSQL_ROOT_PASSWORD=taskflow-root
